services:
  <service-name>:
    restart: unless-stopped
    env_file:
      - .env.prod
    networks:
      service-backbone-shared:
        aliases:
          - <service-name>

networks:
  service-backbone-shared:
    external: true
    name: service-backbone-shared
